# Environment Management

Environments:
- local
- test/CI
- staging
- production

Each environment must have separate credentials and resources where appropriate.

Never use production secrets in local or CI development.
